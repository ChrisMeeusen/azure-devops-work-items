#!/usr/bin/env node

// TODO implement the following control flow:
// validate host platform is supported
// Get the global npm registry location on the host machine
// Determine host machine platform (ie windows, linux, or mac)
// fspath.join to create the link to the platform specific binary
// execute the binary providing the commit message file path, and the repo hooks path
